/**
 * Marca del proyecto — valores por defecto.
 *
 * El nombre real de cada casino lo configura el tenant desde
 * Configuración → Marca (`design.brand.platformName`). Esta constante es
 * solo el **valor de arranque / fallback** cuando todavía no hay uno
 * configurado. Para re-marcar todo el proyecto, cambiá esto en UN solo lugar.
 */

export const DEFAULT_PLATFORM_NAME = 'Casino';
